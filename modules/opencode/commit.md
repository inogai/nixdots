---
description: Commit staged changes with semantic message
agent: build
---

Analyze the staged changes:

!`git diff --staged`

Generate a semantic commit message following conventional commits (feat:, fix:, docs:, etc.) and commit the staged files with that message.
